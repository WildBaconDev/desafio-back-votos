spring:
  datasource:
    url: jdbc:mysql://localhost:3306/votos?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: 'true'

user.service.url: https://user-info.herokuapp.com/users
gerador.app.cpf.url: https://gerador.app/api/cpf/generate

